# One-click deploy on Render: New > Blueprint > point at this repo.
services:
  - type: web
    name: grove
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: node server/index.js
    healthCheckPath: /api/health
    envVars:
      - key: LLM_BASE_URL
        value: https://api.groq.com/openai/v1
      # Each model has its own rate limit, so authoring, checking and grading
      # are deliberately split across different ones.
      - key: LLM_BIG_MODEL
        value: openai/gpt-oss-120b
      - key: LLM_SMALL_MODEL
        value: llama-3.1-8b-instant
      - key: LLM_VERIFY_MODEL
        value: llama-3.1-8b-instant
      - key: LLM_API_KEY
        sync: false        # set this in the Render dashboard, never in the repo
      - key: NODE_VERSION
        value: "22"
